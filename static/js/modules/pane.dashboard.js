;(function(undefined) {

  /**
   * TubeMyNet Dashboard Pane
   * =========================
   *
   * 'Tis not known the purpose of this pane yet, my liege.
   */

  app.panes.dashboard = function() {
    var self = this;

    // Extending
    Pane.call(this,  {
      name: 'dashboard'
    });
  }
}).call(this);
