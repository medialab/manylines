/**
 * TubeMyNet API server unit tests
 * ================================
 *
 */
var assert = require('assert'),
    request = require('supertest');

describe('Space', function() {
  it('is a dummy test', function() {
    assert(true);
  });
});
